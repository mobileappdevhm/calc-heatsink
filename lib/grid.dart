import 'package:flutter/material.dart';

/*
class MyGrid {
  static List<Widget> buildGrid(BuildContext context) {
    List<Widget> buttons = new List();
    buttons.add(buttonOperator("*"));
    List<Widget> list = new List();
    list.add(new Container(
      color: Colors.white30,
      child: new GridView.count(
          crossAxisCount: 4,
          childAspectRatio: 1.0,
          padding: const EdgeInsets.all(4.0),
          mainAxisSpacing: 4.0,
          crossAxisSpacing: 4.0,
          /*
          children: <String>[
            'http://www.for-example.org/img/main/forexamplelogo.png',
          ].map((String url) {
            return new GridTile(child: new Image.network(url, fit: BoxFit.cover));
          }).toList()),
            */
          children: buttons,
      )));
    return list;
  }

  static FloatingActionButton buttonOperator(String operator) {
    return new FloatingActionButton(
        backgroundColor: Colors.orangeAccent,
        child: new Text(operator,
          style: new TextStyle(
              fontWeight: FontWeight.bold,
              fontSize: 25.0,
          ),
        ),
    ));
  }
}
*/
